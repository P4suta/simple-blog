<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>{{ site_title }}</title>
  <id>{{ site_url|safe }}</id>
  <link href="{{ feed_url|safe }}" rel="self"/>
  <link href="{{ site_url|safe }}"/>
  <updated>{{ updated }}</updated>
  {% for entry in entries %}<entry>
    <title>{{ entry.title }}</title><id>{{ entry.url|safe }}</id><link href="{{ entry.url|safe }}"/>
    <published>{{ entry.published }}</published><updated>{{ entry.updated }}</updated>
    <summary>{{ entry.summary }}</summary>
    <content type="html">{{ entry.content_html }}</content>
  </entry>{% endfor %}
</feed>
