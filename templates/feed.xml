<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>{{ title }}</title>
  {% if subtitle %}<subtitle>{{ subtitle }}</subtitle>
  {% endif %}<id>{{ page_url }}</id>
  <link href="{{ feed_url }}" rel="self" type="application/atom+xml"/>
  <link href="{{ page_url }}" rel="alternate" type="text/html"/>
  <updated>{{ updated }}</updated>
  <author><name>{{ author }}</name></author>
  {% for entry in entries %}<entry>
    <title>{{ entry.title }}</title><id>{{ entry.url }}</id><link href="{{ entry.url }}" rel="alternate" type="text/html"/>
    <published>{{ entry.published }}</published><updated>{{ entry.updated }}</updated>
    {% for tag in entry.tags %}<category term="{{ tag.slug }}" label="{{ tag.name }}"/>{% endfor %}
    <summary>{{ entry.summary }}</summary>
    <content type="html">{{ entry.content_html }}</content>
  </entry>{% endfor %}
</feed>
