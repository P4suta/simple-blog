<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url><loc>{{ site_url|safe }}</loc></url>
  {% for entry in entries %}<url><loc>{{ entry.url|safe }}</loc><lastmod>{{ entry.updated }}</lastmod></url>{% endfor %}
</urlset>
